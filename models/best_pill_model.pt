from ultralytics import YOLO

@st.cache_resource
def load_model():
    # Make sure the path is correct
    model_path = "models/best_pill_model.pt"
    model = YOLO(model_path)
    return model

model = load_model()
