import torch
import urllib.request

MODEL_URL = "https://huggingface.co/username/best_pill_model/resolve/main/best_pill_model.pt"
MODEL_PATH = "best_pill_model.pt"

if not os.path.exists(MODEL_PATH):
    urllib.request.urlretrieve(MODEL_URL, MODEL_PATH)

model = YOLO(MODEL_PATH)
