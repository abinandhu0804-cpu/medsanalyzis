from ultralytics import YOLO
model = YOLO("your_original_model.pt")

# Export to a smaller, CPU-friendly version
model.export(format="pt")  # This will produce a smaller .pt compatible with Streamlit
